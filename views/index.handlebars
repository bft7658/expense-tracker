<ul>
  總共花了 ${{totalAmount}}
  {{#each records}}
  <li>
    <i class="{{this.categoryId.icon}} m-2"></i>
    {{this.name}}
    {{this.date}}
    ${{this.amount}}
    <a href="./records/{{ this._id }}/edit">Edit</a>
    <form action="./records/{{ this._id }}/delete" method="POST" style="display: inline;">
      <button type="submit">delete</button>
    </form>
  </li>
  {{/each}}
</ul>
<a href="./records/new">Create</a>